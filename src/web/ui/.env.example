# Where the dev server proxies /api, /webhooks, /health and /ready.
# Only used by `npm run dev` – the production build is served by the API
# process itself and is always same-origin.
VITE_API_TARGET="http://127.0.0.1:3000"

# Port for the Vite dev server.
VITE_PORT="5173"
