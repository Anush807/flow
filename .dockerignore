# Keep the build context small and secrets out of the image.
node_modules
**/node_modules
dist
generated
.git
.gitignore

# Secrets. Configuration is injected as environment variables at runtime.
.env
.env.*
!.env.example

# The console's own dependency tree and previous build are rebuilt in the
# `console` stage; its sources are part of the context.
src/web/ui/dist

.claude
.codex
spec
script
Dockerfile
.dockerignore
*.md
!README.md
